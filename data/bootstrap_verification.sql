-- Read-only verification for data/00_bootstrap.sql.

USE ROLE ACCOUNTADMIN;

SHOW DATABASES LIKE 'PK_AVIATION_TEMPORAL';
SHOW GRANTS OF ROLE RAI_DEMO_AVIATION_TEMPORAL;
SHOW GRANTS TO ROLE RAI_DEMO_AVIATION_TEMPORAL;

USE ROLE RAI_USER;
SHOW DATABASES LIKE 'PK_AVIATION_TEMPORAL';

USE ROLE RAI_DEVELOPER;
SHOW DATABASES LIKE 'PK_AVIATION_TEMPORAL';

USE ROLE ACCOUNTADMIN;
SHOW DATABASES LIKE 'PK_AVIATION_TEMPORAL';

USE ROLE RAI_DEMO_AVIATION_TEMPORAL;
USE WAREHOUSE RAI_XS;
USE DATABASE PK_AVIATION_TEMPORAL;

SELECT
  CURRENT_ORGANIZATION_NAME() AS organization_name,
  CURRENT_ACCOUNT_NAME() AS account_name,
  CURRENT_ACCOUNT() AS account_locator,
  CURRENT_USER() AS user_name,
  CURRENT_ROLE() AS role_name,
  CURRENT_DATABASE() AS database_name,
  CURRENT_WAREHOUSE() AS warehouse_name;

SHOW SCHEMAS IN DATABASE PK_AVIATION_TEMPORAL;
